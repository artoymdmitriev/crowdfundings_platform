<div class="users">
  <h3>Last registered users:</h3>

  <% last_users.each do |user| %>
      <div class="user">
        <div><%= user.email %></div>
        <div><%= user.created_at.strftime("%F %T") %></div>
        <br>
      </div>
  <% end %>
</div>